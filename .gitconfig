[user]
	name = Klaas Gunst
	email = klaasgunst@hotmail.com
[core]
	editor = vim

[credential "https://github.ugent.be"]
    username = kgunst

[credential "https://github.com"]
    username = klgunst
[alias]
	wdiff = diff --color-words --minimal
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
